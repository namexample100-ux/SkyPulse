# Используем официальный легкий образ Python
FROM python:3.10-slim

# Устанавливаем рабочую директорию
WORKDIR /app

# Копируем файл зависимостей
COPY requirements.txt .

# Устанавливаем зависимости
RUN pip install --no-cache-dir -r requirements.txt

# Копируем все файлы проекта
COPY . .

# Hugging Face ожидает, что приложение будет слушать порт 7860.
# Для телеграм-бота (polling) это не обязательно, но полезно для статуса "Running".
# Если захочешь добавить мини-веб-сервер для статуса — порт 7860.
EXPOSE 7860

# Запускаем бота
CMD ["python", "bot.py"]
